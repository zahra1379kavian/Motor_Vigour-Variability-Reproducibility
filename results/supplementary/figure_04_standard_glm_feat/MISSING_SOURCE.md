# Supplementary Figure 4 Source Missing

The requested source image was not found in the current repository:

```text
outputs/feat/mixed_model.gfeat/cope1.feat/rendered_thresh_zstat1_z3p1_brain_slices_whitebg_top_RL.png
```

I also checked for matching `.feat`/`.gfeat` folders and this rendered PNG
outside and inside `results9s`; no matching file or FEAT directory was present.

When the FEAT output is available, copy the rendered image to:

```text
figures/supplementary/supp_figure_04_standard_glm_feat_brain_slices.png
```

and place the compact FEAT design/statistical files under this folder.
